<h1>Wikinimous</h1>

<% @articles.each do |article| %>
<%= article.title %><%= link_to "Delete", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to "Edit", edit_article_path(article) %>
<%= link_to "Details", article_path(article), method: :get %>
<br>
<% end %>
<br>
<%= link_to "Add a new article", articles_new_path %>
